---
phase: 02-locale-persistence-and-formatting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aicodexml-web/src/app/shared/services/locale.service.ts
  - aicodexml-web/src/app/webapp-common/user-preferences.ts
  - aicodexml-web/src/app/layout/header/header-user-menu-actions/header-user-menu-actions.component.ts
autonomous: true
requirements: [PREF-01, PREF-02]
user_setup: []
must_haves:
  truths:
    - "Signed-in language choice is stored through the existing user-preference API instead of remaining local-storage-only."
    - "A saved server-side language overrides local/browser fallback after authenticated preferences load."
    - "A user without saved language keeps the already resolved language and that value is backfilled into preferences."
  artifacts:
    - path: "aicodexml-web/src/app/shared/services/locale.service.ts"
      provides: "Language normalization, preference-path helpers, and reconciliation behavior"
    - path: "aicodexml-web/src/app/webapp-common/user-preferences.ts"
      provides: "Authenticated preference reconciliation and backfill"
    - path: "aicodexml-web/src/app/layout/header/header-user-menu-actions/header-user-menu-actions.component.ts"
      provides: "Persisted language changes from the existing switcher"
  key_links:
    - "startup language can change once authenticated preferences load"
    - "language changes in the existing header switcher enqueue server persistence when preferences are ready"
---

<objective>
Persist locale via existing user preferences and reconcile startup precedence.

Purpose: Make language durable without adding a new backend contract.
Output: Locale preference key, reconciliation logic, and persisted writes from the switcher.
</objective>

<execution_context>
@/Users/yangjianbo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yangjianbo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-locale-persistence-and-formatting/02-CONTEXT.md
@.planning/phases/02-locale-persistence-and-formatting/02-RESEARCH.md
@aicodexml-web/src/app/shared/services/locale.service.ts
@aicodexml-web/src/app/webapp-common/user-preferences.ts
@aicodexml-web/src/app/layout/header/header-user-menu-actions/header-user-menu-actions.component.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define locale preference semantics and reconciliation helpers</name>
  <files>aicodexml-web/src/app/shared/services/locale.service.ts</files>
  <action>Add a stable preference path and helpers that normalize persisted language values, expose the app locale mapping needed by later formatting work, and reconcile a loaded preference against the current language.</action>
  <verify>cd aicodexml-web && npm run build</verify>
  <done>Locale service can safely interpret server preference values and expose one authoritative active language mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile and backfill locale from authenticated user preferences</name>
  <files>aicodexml-web/src/app/webapp-common/user-preferences.ts</files>
  <action>After preferences load, apply a saved language if it exists. If not, preserve the already active language and write it back into the existing preference document using the current debounced persistence mechanism.</action>
  <verify>cd aicodexml-web && npm run build</verify>
  <done>Authenticated refresh and later sessions have a durable locale path while users without a saved preference reconcile cleanly.</done>
</task>

<task type="auto">
  <name>Task 3: Persist future language changes from the current switcher</name>
  <files>aicodexml-web/src/app/layout/header/header-user-menu-actions/header-user-menu-actions.component.ts</files>
  <action>Update the existing header language switcher so that once preferences are ready it writes the selected language into user preferences in addition to local storage.</action>
  <verify>cd aicodexml-web && npm run build</verify>
  <done>Changing language while signed in updates both the active runtime language and the durable user preference.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd aicodexml-web && npm run build` passes
- [ ] Locale preference path is used consistently in touched files
- [ ] No touched persistence logic depends on a new backend API
</verification>

<success_criteria>

- All tasks completed
- Server preference wins after login when present
- Local/browser-resolved language backfills when server preference is absent
- Existing switcher writes durable preference for signed-in users

</success_criteria>

<output>
After completion, create `.planning/phases/02-locale-persistence-and-formatting/02-01-SUMMARY.md`
</output>
