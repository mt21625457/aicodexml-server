---
phase: 04-feature-module-localization-sweep
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - aicodexml-web/src/app/webapp-common/experiments/**
  - aicodexml-web/src/app/webapp-common/experiments-compare/**
  - aicodexml-web/src/app/webapp-common/models/**
  - aicodexml-web/src/app/webapp-common/reports/**
  - aicodexml-web/src/assets/i18n/en.json
  - aicodexml-web/src/assets/i18n/zh-CN.json
autonomous: true
requirements: [FEAT-01]
user_setup: []
must_haves:
  truths:
    - "Experiments, models, and reports no longer expose the targeted hard-coded English labels in the touched flows."
    - "Dialog actions, compare headers, table actions, and model/report detail labels follow the active locale."
  artifacts:
    - path: "aicodexml-web/src/assets/i18n/en.json"
      provides: "English feature keys for experiments/models/reports"
    - path: "aicodexml-web/src/assets/i18n/zh-CN.json"
      provides: "Simplified Chinese feature keys for experiments/models/reports"
  key_links:
    - "04-03 can reuse the same TypeScript translation patterns for serving and settings admin flows"
---

<objective>
Localize experiment/task/model/report feature surfaces and shared subflows.
</objective>

<context>
@.planning/phases/04-feature-module-localization-sweep/04-CONTEXT.md
@.planning/phases/04-feature-module-localization-sweep/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand catalogs for experiments, models, and reports</name>
  <files>aicodexml-web/src/assets/i18n/en.json, aicodexml-web/src/assets/i18n/zh-CN.json</files>
  <action>Add structured keys for experiments/models/reports before touching templates and TypeScript labels.</action>
  <verify>cd aicodexml-web && npm run build</verify>
  <done>Catalog coverage exists for the 04-02 feature slice.</done>
</task>

<task type="auto">
  <name>Task 2: Localize experiments and compare flows</name>
  <files>aicodexml-web/src/app/webapp-common/experiments/**, aicodexml-web/src/app/webapp-common/experiments-compare/**</files>
  <action>Translate experiment detail labels, compare headers, runtime menus, and queue/clone dialogs, using `TranslateService` where labels are generated in TypeScript.</action>
  <verify>cd aicodexml-web && npm run build</verify>
  <done>Experiment and compare flows render the targeted copy from catalogs.</done>
</task>

<task type="auto">
  <name>Task 3: Localize models and reports</name>
  <files>aicodexml-web/src/app/webapp-common/models/**, aicodexml-web/src/app/webapp-common/reports/**</files>
  <action>Translate model detail panels, report dialogs, report empty states, headers, and table actions while keeping entity content literal.</action>
  <verify>cd aicodexml-web && npm run build</verify>
  <done>Model and report surfaces no longer rely on the targeted English UI literals.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/04-feature-module-localization-sweep/04-02-SUMMARY.md`
</output>
