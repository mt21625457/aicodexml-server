---
phase: 05-verification-and-guardrails
plan: 02
type: execute
wave: 5
depends_on: [01]
files_modified:
  - .planning/phases/05-verification-and-guardrails/**
  - aicodexml-web/package.json
  - aicodexml-web/scripts/**
  - aicodexml-web/README.md
autonomous: true
requirements: [QA-01]
user_setup: []
must_haves:
  truths:
    - "The repo has a repeatable local guardrail that flags newly introduced hard-coded UI strings in the Angular app."
    - "Developers have written guidance for how to add new localized UI copy without bypassing the translation system."
  artifacts:
    - path: ".planning/phases/05-verification-and-guardrails/05-02-SUMMARY.md"
      provides: "Summary of the new regression guardrail and contributor guidance"
---

<objective>
Add maintainability guardrails so new UI work does not regress to hard-coded or mixed-language copy.
</objective>

<context>
@.planning/phases/05-verification-and-guardrails/05-CONTEXT.md
@.planning/phases/05-verification-and-guardrails/05-RESEARCH.md
@.planning/phases/05-verification-and-guardrails/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a lightweight hard-coded-string detection script</name>
  <files>aicodexml-web/package.json, aicodexml-web/scripts/**</files>
  <action>Create a repo-native scan that focuses on likely user-visible Angular template and TS UI surfaces while excluding known-safe exception patterns.</action>
  <verify>cd aicodexml-web && npm run build</verify>
  <done>A repeatable local scan exists and can be run by contributors before merging UI changes.</done>
</task>

<task type="auto">
  <name>Task 2: Document localization development rules and exceptions</name>
  <files>.planning/phases/05-verification-and-guardrails/**, aicodexml-web/README.md</files>
  <action>Write concise contributor guidance covering when to use `TranslatePipe`, when to use `TranslateService.instant(...)`, and what string patterns the guardrail intentionally ignores.</action>
  <verify>cd aicodexml-web && npm run build</verify>
  <done>The guardrail is documented and future multilingual work has an explicit operating model.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/05-verification-and-guardrails/05-02-SUMMARY.md`
</output>
