---
phase: 05-verification-and-guardrails
plan: 01
type: execute
wave: 4
depends_on: [04]
files_modified:
  - .planning/phases/05-verification-and-guardrails/**
  - aicodexml-web/src/app/**
  - aicodexml-web/src/assets/i18n/**
autonomous: true
requirements: [QA-01]
user_setup: []
must_haves:
  truths:
    - "Bilingual verification evidence exists for the highest-value routes and flows that are reachable locally."
    - "Any unreachable authenticated workflow is documented as a concrete blocker instead of being implicitly assumed verified."
    - "Any mixed-language blockers discovered during verification are either fixed in-place if low risk or captured as explicit follow-up gaps."
  artifacts:
    - path: ".planning/phases/05-verification-and-guardrails/05-01-SUMMARY.md"
      provides: "Verification evidence, blockers, and follow-up notes for the bilingual rollout"
---

<objective>
Execute bilingual regression verification across critical workflows and document the actual evidence.
</objective>

<context>
@.planning/phases/05-verification-and-guardrails/05-CONTEXT.md
@.planning/phases/05-verification-and-guardrails/05-RESEARCH.md
@.planning/phases/05-verification-and-guardrails/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a verification matrix and establish reachable runtime scope</name>
  <files>.planning/phases/05-verification-and-guardrails/**</files>
  <action>Enumerate the critical bilingual workflows, list what can be verified via static/build checks, and determine whether a proxied local runtime is available for browser verification.</action>
  <verify>cd aicodexml-web && npm run build</verify>
  <done>A concrete verification matrix exists and any runtime blockers are explicit.</done>
</task>

<task type="auto">
  <name>Task 2: Run automated bilingual regression checks</name>
  <files>.planning/phases/05-verification-and-guardrails/**, aicodexml-web/src/app/**, aicodexml-web/src/assets/i18n/**</files>
  <action>Run production build plus focused literal scans for known mixed-language regressions, fixing any low-risk misses found during verification.</action>
  <verify>cd aicodexml-web && npm run build</verify>
  <done>Automated verification evidence is captured and low-risk issues found during the pass are resolved.</done>
</task>

<task type="auto">
  <name>Task 3: Perform browser/manual spot checks for reachable flows</name>
  <files>.planning/phases/05-verification-and-guardrails/**, aicodexml-web/src/app/**</files>
  <action>Use the best available local runtime to inspect login and reachable product routes in both `en` and `zh-CN`; if the authenticated environment is unavailable, document the exact blocker and any deferred coverage.</action>
  <verify>cd aicodexml-web && npm run build</verify>
  <done>Phase 5 has real bilingual evidence for reachable flows and a precise blocker record for anything not reachable locally.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/05-verification-and-guardrails/05-01-SUMMARY.md`
</output>
