---
phase: 01-i18n-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - aicodexml-web/package.json
  - aicodexml-web/src/app/app.module.ts
  - aicodexml-web/src/app/core/app-init.ts
  - aicodexml-web/src/app/shared/services/locale.service.ts
  - aicodexml-web/src/assets/i18n/en.json
  - aicodexml-web/src/assets/i18n/zh-CN.json
autonomous: true
requirements: [I18N-01, I18N-03]
user_setup: []
must_haves:
  truths:
    - "The Angular app boots with one runtime translation architecture rather than separate per-locale builds."
    - "English and Simplified Chinese catalogs exist and can be loaded at runtime."
    - "Core shell text has a safe fallback path when translations are missing."
  artifacts:
    - path: "aicodexml-web/src/app/shared/services/locale.service.ts"
      provides: "Central locale and translation orchestration"
    - path: "aicodexml-web/src/assets/i18n/en.json"
      provides: "English runtime catalog"
    - path: "aicodexml-web/src/assets/i18n/zh-CN.json"
      provides: "Simplified Chinese runtime catalog"
  key_links:
    - "app bootstrap initializes the locale service before shared shell rendering depends on translated strings"
    - "translation catalogs are reachable through the frontend build output"
---

<objective>
Integrate the runtime i18n foundation into the Angular application.

Purpose: Lock the architecture early so later translation work is additive rather than a rewrite.
Output: Translation dependencies, locale service, catalog files, and bootstrap wiring.
</objective>

<execution_context>
@/Users/yangjianbo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yangjianbo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-i18n-foundation/01-CONTEXT.md
@.planning/phases/01-i18n-foundation/01-RESEARCH.md
@aicodexml-web/package.json
@aicodexml-web/angular.json
@aicodexml-web/src/app/app.module.ts
@aicodexml-web/src/app/core/app-init.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime translation dependencies and locale scaffolding</name>
  <files>aicodexml-web/package.json, aicodexml-web/src/app/shared/services/locale.service.ts</files>
  <action>Install and wire a runtime translation library compatible with Angular 20. Create a locale service that defines supported languages, default and fallback language behavior, active language state, and catalog loading hooks. Keep the abstraction app-owned so later persistence and formatting work can build on it cleanly.</action>
  <verify>cd aicodexml-web && npm install && npm run lint</verify>
  <done>Dependencies are declared and a central locale service exists with runtime language orchestration responsibilities.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap locale initialization into the Angular app</name>
  <files>aicodexml-web/src/app/app.module.ts, aicodexml-web/src/app/core/app-init.ts</files>
  <action>Wire the locale service into app bootstrap so the initial language is resolved before shared shell UI depends on translated strings. Ensure the app chooses one runtime strategy and does not drift toward separate locale builds. Add safe missing-translation fallback behavior.</action>
  <verify>cd aicodexml-web && npm run build</verify>
  <done>App bootstrap initializes runtime i18n successfully and core shell text no longer depends on hard-coded single-language assumptions.</done>
</task>

<task type="auto">
  <name>Task 3: Seed bilingual runtime catalogs for the shared shell baseline</name>
  <files>aicodexml-web/src/assets/i18n/en.json, aicodexml-web/src/assets/i18n/zh-CN.json</files>
  <action>Create structured English and Simplified Chinese catalog files with the first shared-shell keys needed to prove the runtime path works. Use a stable, nested key convention that later phases can extend without churn.</action>
  <verify>cd aicodexml-web && npm run build</verify>
  <done>Both language catalogs exist, are included in the frontend asset tree, and contain the shared-shell baseline entries needed by Phase 1.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd aicodexml-web && npm run lint` passes
- [ ] `cd aicodexml-web && npm run build` passes
- [ ] Translation catalogs are present in the built asset output
</verification>

<success_criteria>

- All tasks completed
- Runtime translation baseline exists in the Angular app
- Fallback behavior is defined and wired
- Catalog structure is ready for later shell and feature migration

</success_criteria>

<output>
After completion, create `.planning/phases/01-i18n-foundation/01-01-SUMMARY.md`
</output>
