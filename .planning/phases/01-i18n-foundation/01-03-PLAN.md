---
phase: 01-i18n-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docker/build/Dockerfile
  - docker/build/internal_files/entrypoint.sh
  - docker/build/internal_files/update_from_env.py
  - docker/build/internal_files/clearml.conf.template
autonomous: true
requirements: [BUILD-01]
user_setup: []
must_haves:
  truths:
    - "The server repository packages the localized frontend from the local `aicodexml-web` submodule."
    - "Frontend translation assets remain available in the packaged web output."
    - "Runtime web configuration is ready to expose default/supported language metadata without breaking existing startup."
  artifacts:
    - path: "docker/build/Dockerfile"
      provides: "Localized frontend packaging path"
    - path: "docker/build/internal_files/update_from_env.py"
      provides: "Runtime config injection point for locale metadata"
  key_links:
    - "Docker build consumes the local submodule and ships translation-capable frontend assets"
    - "Runtime config injection can pass locale defaults to the packaged web app"
---

<objective>
Align server-side packaging and runtime configuration with the localized frontend foundation.

Purpose: Prevent Phase 1 from succeeding locally but failing in the actual Dockerized delivery path.
Output: Packaging and runtime-config readiness for the multilingual frontend.
</objective>

<execution_context>
@/Users/yangjianbo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yangjianbo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-i18n-foundation/01-CONTEXT.md
@.planning/phases/01-i18n-foundation/01-RESEARCH.md
@.planning/phases/01-i18n-foundation/01-01-SUMMARY.md
@docker/build/Dockerfile
@docker/build/internal_files/entrypoint.sh
@docker/build/internal_files/update_from_env.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm translation assets survive the frontend build and server packaging path</name>
  <files>docker/build/Dockerfile</files>
  <action>Review and adjust the Docker packaging path as needed so the runtime translation-capable frontend output from `aicodexml-web` is copied into the final web image without dropping catalog files or related assets.</action>
  <verify>docker build -f docker/build/Dockerfile .</verify>
  <done>The server build path clearly packages the localized frontend from the local submodule.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare runtime web configuration for locale metadata</name>
  <files>docker/build/internal_files/entrypoint.sh, docker/build/internal_files/update_from_env.py, docker/build/internal_files/clearml.conf.template</files>
  <action>Add or prepare runtime configuration fields for default language and supported languages in the existing web configuration injection flow, without breaking current deployments. Keep this foundational so later phases can consume the metadata cleanly.</action>
  <verify>python3 docker/build/internal_files/update_from_env.py --help || true</verify>
  <done>Runtime configuration injection has a clear, non-breaking path for locale defaults and supported language metadata.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Docker/web packaging path still references the local `aicodexml-web` submodule correctly
- [ ] Runtime config injection changes are syntactically valid
- [ ] Localized frontend assets remain packageable in the delivery flow
</verification>

<success_criteria>

- All tasks completed
- Packaging path supports the multilingual frontend foundation
- Runtime config path is ready for locale metadata
- No deployment-breaking regression introduced

</success_criteria>

<output>
After completion, create `.planning/phases/01-i18n-foundation/01-03-SUMMARY.md`
</output>
